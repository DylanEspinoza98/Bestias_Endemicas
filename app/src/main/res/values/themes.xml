<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BestiasEndemicas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BestiasEndemicas" parent="Base.Theme.BestiasEndemicas" />

    <!-- Codigo que permite la capacidad de darle un borde blanco a un texto -->
    <style name="TextViewConBordeBlanco">
        <item name="android:textColor">@android:color/black</item> <!-- Color principal del texto -->
        <item name="android:shadowColor">@android:color/white</item> <!-- Color del borde -->
        <item name="android:shadowDx">0</item> <!-- Desplazamiento horizontal del borde -->
        <item name="android:shadowDy">0</item> <!-- Desplazamiento vertical del borde -->
        <item name="android:shadowRadius">12</item> <!-- Grosor/Desenfoque del borde -->
    </style>

    <!-- Codigo que permite la capacidad de no repetir un codigo y simplemente se hara un llamado-->
    <style name="SocialMediaButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>
</resources>